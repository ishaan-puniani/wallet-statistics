import React from "react";
import ReactDOM from "react-dom";
import UserAchievement from "./UserAchievements";
export const render = (container: any) => {
    ReactDOM.render(<UserAchievement />, container);
};
